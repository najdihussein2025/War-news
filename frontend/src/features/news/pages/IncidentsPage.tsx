import { EmptyState } from "../../../components/ui";

export const IncidentsPage = () => <section className="rounded-lg border border-border bg-surface-raised"><EmptyState title="Incident API not available" description="Sample incidents were removed so this page cannot be mistaken for live data. Add authenticated incident list, filtering, sorting, and pagination endpoints to enable this screen." /></section>;
